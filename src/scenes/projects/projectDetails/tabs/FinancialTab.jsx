import { tokens } from "../../../../theme";


import {
  Box,
  Typography,
  useTheme,
} from "@mui/material";


const FinancialTab = ({ participants }) => {
  const theme = useTheme();
  const colors = tokens(theme.palette.mode);



  return (
    <Box m="10px">
  
    </Box>
  );
};

export default FinancialTab;



